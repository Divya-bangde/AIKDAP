"""Papers module.

Bibliographic identity for the PDF documents in a project: each paper's
OpenAlex match (id, DOI, year, citation count, and the works it
references), stored in `paper_references`, one row per document asset.
This is what the project paper map is built from.
"""
